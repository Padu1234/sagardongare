package string;

public class StringBylitral {
public static void main(String[]args) {
	String a= "pradip";   //                  litral type to define string syntax 
	//    backend process for litral referance (a) can check the object inside scp if object is already their then it directly point to 
    //  that object if not then create object inside scp.
	System.out.println(a);  
	
}
}
